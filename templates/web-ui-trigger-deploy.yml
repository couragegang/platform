# Устаревший путь: web-ui → couragegang/ui (monorepo).
# Актуальный trigger-deploy: см. couragegang/ui/.github/workflows/trigger-deploy.yml
# (workflow_dispatch + PLATFORM_DISPATCH_TOKEN, path filter apps/web).
