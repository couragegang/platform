# Оверлей для контура test на том же VPS, что и prod (другие host-порты).
# Используется: COMPOSE_FILE=docker-compose.yml:docker-compose.ports-test.yml

services:
  iam:
    ports: ["18080:8080"]
  mcp:
    ports: ["18081:8081"]
  bff:
    ports: ["18082:8082"]
  ai:
    ports: ["18083:8083"]
  config:
    ports: ["18084:8084"]
  policy:
    ports: ["18085:8085"]
  audit:
    ports: ["18086:8086"]
  secrets:
    ports: ["18087:8087"]
  knowledge:
    ports: ["18088:8088"]
